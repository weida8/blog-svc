import {dbUserName, dbPassword} from './config';

export const dbName  = `mongodb+srv://${dbUserName}:${dbPassword}@cluster0.5rrsk.mongodb.net/dev`;

export const issuer = 'testNode'
export const subject = 'weidapan8@gmail.com'
